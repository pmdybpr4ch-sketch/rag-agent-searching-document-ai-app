# Streamlit Secrets example -- do NOT commit real secrets

# OpenAI / OpenAI-compatible API key
OPENAI_API_KEY = "your-openai-api-key-here"

# Other optional keys used by the app
# Example: GOOGLE_API_KEY = "..."

# The real secrets should be set in Streamlit Cloud dashboard (Advanced settings -> Secrets)
